import ProductDetailPage, { generateMetadata as generateProductMetadata } from '@/app/product/[slug]/page';

export const generateMetadata = generateProductMetadata;

export default ProductDetailPage;
